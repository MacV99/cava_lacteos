Run `git diff --staged` and `git log --oneline -5` to understand the changes and recent commit style.

Generate a single commit message in this exact format:
- One line only
- English
- Pattern: `type: short description`
- Types: feat, fix, refactor, docs, chore
- Max 72 characters
- No period at the end

Print only the commit message, nothing else.
